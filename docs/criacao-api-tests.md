Objetivo:
Implementar a automação de testes funcionais de API para o endpoint GET /health.

Contexto:
- A especificação da API para o endpoint GET /health encontra-se no api-docs

Regras:
- Utilize JavaScript, Mocha, Supertest e Chai.
- Os testes devem ser alocados em uma pasta com o mesmo nome do endpoint, dentro do diretório `test`.